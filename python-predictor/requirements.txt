grpcio==1.68.0
grpcio-tools==1.68.0
protobuf==5.28.3
prometheus-client==0.21.0
# Stage G: real model training/inference (logistic regression via gradient descent — no scikit-learn
# is pinned here or available in the environment this was developed in). Verified locally against
# Python 3.14; NOT yet verified against this Dockerfile's python:3.11-slim base image — the Docker
# build in this environment could not be run (no daemon available). Confirm this version installs
# cleanly in the actual container before relying on it in production.
numpy==2.1.3
# Stage H: real gradient-boosted-tree failure classifier (raw Booster API, no scikit-learn wrapper).
# Verified locally (Python 3.14, installs and trains/predicts correctly via a real train->serve round
# trip) but, like numpy above, NOT yet verified against this Dockerfile's python:3.11-slim base image —
# no Docker daemon available in this environment. If the wheel's bundled OpenMP runtime fails to import
# on a truly minimal base image, `apt-get install -y libgomp1` is the standard fix — confirm before
# relying on this in production.
xgboost==3.4.0
# Stage I: real LSTM load-forecasting model (torch.nn.LSTM). A materially larger dependency than
# anything else in this project (~200MB+ install, longer Docker build) — CPU-only wheel, installed from
# https://download.pytorch.org/whl/cpu (see Dockerfile). Verified locally (Python 3.14, real
# train->serve round trip converges on synthetic data) but, like numpy/xgboost above, NOT yet verified
# against this Dockerfile's python:3.11-slim base image — no Docker daemon available in this environment.
# Confirm the install and image size are acceptable before relying on this in production.
torch==2.13.0
